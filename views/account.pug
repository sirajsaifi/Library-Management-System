extends base

mixin navItem(link, text, icon, active)
    li(class = `${active ? 'active' : ''}`)
        a(href=`${link}`) 
            | #{text}
            img.navItems-icons(src=`img/icons/${icon}.png`, alt=`${icon}`)

block content 
    #main-content 
        #user-view
            #user-account-menu
                if (user.role === 'staff')
                    ul.side-nav 
                        +navItem('/me', 'Settings', 'settings')

                else if (user.role === 'student')
                    ul.side-nav 
                        +navItem('/me', 'Settings', 'settings')
                        +navItem('/my-books', 'My Books', 'booksIssued')

                else if (user.role === 'librarian')
                    ul.side-nav 
                        +navItem('/me', 'Settings', 'settings')
                        +navItem('/create-user', 'Create User', 'create-user')
                        +navItem('/students', 'Students', 'students')
                        +navItem('/staff', 'Staff', 'staff')
                        +navItem('/books', 'Books', 'books')
                        +navItem('/books-issued', 'Books Issued', 'booksIssued')

            block user-account